import { expect } from 'chai';

import navReducer from '../nav-reducer';

describe('navReducer', () => {
  it('should return state with next route', () => {
    const nextState = navReducer();
  });
});
